<Window x:Class="Poe2DbLookup.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="560"
        Height="420"
        MinWidth="560"
        MinHeight="420"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Icon="Assets/app-icon.ico"
        Background="#0F172A"
        Foreground="#F8FAFC"
        ShowInTaskbar="False"
        PreviewKeyDown="Window_PreviewKeyDown">
  <Window.Resources>
    <SolidColorBrush x:Key="PanelBorder" Color="#334155" />
    <SolidColorBrush x:Key="MutedText" Color="#94A3B8" />
    <SolidColorBrush x:Key="ButtonBackground" Color="#1E293B" />

    <Style TargetType="{x:Type Button}">
      <Setter Property="MinHeight" Value="36" />
      <Setter Property="Padding" Value="14,7" />
      <Setter Property="Foreground" Value="#F8FAFC" />
      <Setter Property="Background" Value="{StaticResource ButtonBackground}" />
      <Setter Property="BorderBrush" Value="#475569" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border x:Name="Chrome"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="7"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Chrome" Property="Background" Value="#263449" />
              </Trigger>
              <Trigger Property="IsDefaulted" Value="True">
                <Setter TargetName="Chrome" Property="BorderBrush" Value="#60A5FA" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border Margin="18"
          Background="#111827"
          BorderBrush="{StaticResource PanelBorder}"
          BorderThickness="1"
          CornerRadius="10"
          Padding="20">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <TextBlock Text="设置"
                 FontSize="20"
                 FontWeight="SemiBold"
                 Margin="0,0,0,18" />

      <Grid Grid.Row="1">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="108" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="呼出快捷键"
                   Foreground="{StaticResource MutedText}"
                   VerticalAlignment="Center"
                   Margin="0,0,0,12" />
        <Button x:Name="ActivationHotkeyButton"
                Grid.Column="1"
                Margin="0,0,0,12"
                HorizontalContentAlignment="Left"
                Click="ActivationHotkeyButton_Click" />

        <TextBlock Grid.Row="1"
                   Text="刷新快捷键"
                   Foreground="{StaticResource MutedText}"
                   VerticalAlignment="Center"
                   Margin="0,0,0,12" />
        <Button x:Name="RefreshHotkeyButton"
                Grid.Row="1"
                Grid.Column="1"
                Margin="0,0,0,12"
                HorizontalContentAlignment="Left"
                Click="RefreshHotkeyButton_Click" />

        <CheckBox x:Name="StartWithWindowsCheckBox"
                  Grid.Row="2"
                  Grid.ColumnSpan="2"
                  Content="开机启动"
                  Foreground="#F8FAFC"
                  Margin="0,4,0,0" />
      </Grid>

      <TextBlock x:Name="StatusText"
                 Grid.Row="2"
                 Foreground="{StaticResource MutedText}"
                 FontSize="12"
                 MinHeight="18"
                 Margin="0,10,0,14" />

      <StackPanel Grid.Row="3"
                  Orientation="Horizontal"
                  HorizontalAlignment="Right">
        <Button Content="取消"
                MinWidth="84"
                Margin="0,0,10,0"
                IsCancel="True" />
        <Button Content="保存"
                MinWidth="84"
                IsDefault="True"
                Click="SaveButton_Click" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
